---
name: start
description: "开始会话"
---

# 开始会话

初始化您的 AI 开发会话并开始处理任务。

---

## 操作类型

| 标记 | 含义 | 执行者 |
|--------|---------|----------|
| `[AI]` | 由 AI 执行的 Bash 脚本或工具调用 | 您（AI） |
| `[USER]` | 由用户执行的技能 | 用户 |

---

## 初始化 `[AI]`

### 步骤 1：了解开发工作流

首先，阅读工作流指南以了解开发过程：

```bash
cat .trellis/workflow.md
```

**遵循 workflow.md 中的说明** - 它包含：
- 核心原则（先读后写、遵循标准等）
- 文件系统结构
- 开发过程
- 最佳实践

### 步骤 2：获取当前上下文

```bash
python3 ./.trellis/scripts/get_context.py
```

这显示：开发人员身份、git 状态、当前任务（如有）、活动任务。

### 步骤 3：阅读指南索引

```bash
cat .trellis/spec/frontend/index.md  # 前端指南
cat .trellis/spec/backend/index.md   # 后端指南
cat .trellis/spec/guides/index.md    # 思维指南
```

### 步骤 4：报告并询问

报告您学到的并询问："您想做什么工作？"

---

## 任务分类

当用户描述任务时，对其进行分类：

| 类型 | 标准 | 工作流 |
|------|----------|----------|
| **问题** | 用户询问代码、架构或某事如何运作 | 直接回答 |
| **简单修复** | 拼写错误修复、注释更新、单行更改、< 5 分钟 | 直接编辑 |
| **简单任务** | 目标清晰，1-2 个文件，范围明确 | 快速确认 → 任务工作流 |
| **复杂任务** | 目标模糊、多个文件、架构决策 | **头脑风暴 → 任务工作流** |

### 决策规则

> **如果有疑问，使用头脑风暴 + 任务工作流。**
>
> 任务工作流确保代码规范被注入到正确的上下文，从而产生更高质量的代码。
> 开销很小，但收益显著。

> **子任务分解**：如果头脑风暴揭示多个独立工作项，考虑使用 `--parent` 标志或 `add-subtask` 命令创建子任务。
> 详见头脑风暴技能的步骤 8。

---

## 问题 / 简单修复

对于问题或简单修复，直接工作：

1. 回答问题或进行修复
2. 如果代码有更改，提醒用户运行 `$finish-work`

---

## 简单任务

对于简单、定义良好的任务：

1. 快速确认："我理解您想[目标]。准备继续吗？"
2. 如果是，继续到**任务工作流第一阶段路径 B**（创建任务、写 PRD，然后研究）
3. 如果不是，澄清并再次确认

---

## 复杂任务 - 先头脑风暴

对于复杂或模糊的任务，使用头脑风暴过程来澄清需求。

参见 `$brainstorm` 获取完整流程。总结：

1. **确认并分类** - 陈述您的理解
2. **创建任务目录** - 在 `prd.md` 中跟踪不断演变的需求
3. **一次问一个问题** - 每次回答后更新 PRD
4. **提出方法** - 对于架构决策
5. **确认最终需求** - 获得明确批准
6. **继续任务工作流** - 在 PRD 中有明确需求

---

## 任务工作流（开发任务）

**为什么使用此工作流**？
- 在编码前运行专门的研究轮次
- 在 jsonl 上下文中配置规范
- 使用注入的上下文实施
- 用单独的检查轮次验证
- 结果：自动遵循项目约定的代码

### 概述：两个入口点

```
从头脑风暴（复杂任务）：
  PRD 确认 → 研究 → 配置上下文 → 激活 → 实现 → 检查 → 完成

从简单任务：
  确认 → 创建任务 → 写 PRD → 研究 → 配置上下文 → 激活 → 实现 → 检查 → 完成
```

**关键原则：研究发生在需求明确之后（PRD 存在）。**

---

### 第一阶段：建立需求

#### 路径 A：从头脑风暴（跳过到第二阶段）

PRD 和任务目录已从头脑风暴存在。直接跳到第二阶段。

#### 路径 B：从简单任务

**步骤 1：确认理解** `[AI]`

快速确认：
- 目标是什么？
- 什么类型的开发？（前端 / 后端 / 全栈）
- 有什么具体要求或约束？

如果不清楚，询问澄清问题。

**步骤 2：创建任务目录** `[AI]`

```bash
TASK_DIR=$(python3 ./.trellis/scripts/task.py create "<title>" --slug <name>)
```

**步骤 3：写 PRD** `[AI]`

在任务目录中创建 `prd.md`：

```markdown
# <任务标题>

## 目标
<我们要实现什么>

## 需求
- <需求 1>
- <需求 2>

## 验收标准
- [ ] <标准 1>
- [ ] <标准 2>

## 技术笔记
<任何技术决策或约束>
```

---

### 第二阶段：准备实施（共享）

> 两条路径在此汇聚。在继续之前，PRD 和任务目录必须存在。

**步骤 4：代码规范深度检查** `[AI]`

如果任务涉及基础设施或跨层契约，在定义代码规范深度之前不要开始实施。

当变更包含以下任何一项时触发此要求：
- 新或更改的命令/API 签名
- 数据库模式或迁移更改
- 基础设施集成（存储、队列、缓存、密钥、环境契约）
- 跨层负载转换

继续之前必须有：
- [ ] 确定要更新的目标代码规范文件
- [ ] 定义具体契约（签名、字段、环境密钥）
- [ ] 定义验证和错误矩阵
- [ ] 至少定义一个良好/基础/糟糕案例

**步骤 5：研究代码库** `[AI]`

根据确认的 PRD，运行专门的研究并产生：

1. `.trellis/spec/` 中的相关规范文件
2. 要遵循的现有代码模式（2-3 个示例）
3. 可能需要修改的文件

使用此输出格式：

```markdown
## 相关规范
- <路径>：<为什么相关>

## 发现的代码模式
- <模式>：<示例文件路径>

## 要修改的文件
- <路径>：<什么更改>
```

**步骤 6：配置上下文** `[AI]`

初始化默认上下文：

```bash
python3 ./.trellis/scripts/task.py init-context "$TASK_DIR" <type>
# type: backend | frontend | fullstack
```

添加研究轮次中找到的规范：

```bash
# 对于每个相关规范和代码模式：
python3 ./.trellis/scripts/task.py add-context "$TASK_DIR" implement "<path>" "<reason>"
python3 ./.trellis/scripts/task.py add-context "$TASK_DIR" check "<path>" "<reason>"
```

**步骤 7：激活任务** `[AI]`

```bash
python3 ./.trellis/scripts/task.py start "$TASK_DIR"
```

这设置 `.current-task` 以便 hooks 可以注入上下文。

---

### 第三阶段：执行（共享）

**步骤 8：实施** `[AI]`

实施 `prd.md` 中描述的任务。

- 遵循注入到实施上下文的所有规范
- 保持更改范围在需求内
- 在完成前运行 lint 和 typecheck

**步骤 9：检查质量** `[AI]`

针对检查上下文运行质量轮次：

- 根据规范审查所有代码更改
- 直接修复问题
- 确保 lint 和 typecheck 通过

**步骤 10：完成** `[AI]`

1. 验证 lint 和 typecheck 通过
2. 报告实施了什么
3. 提醒用户：
   - 测试更改
   - 准备就绪时提交
   - 运行 `$record-session` 记录此会话

---

## 继续现有任务

如果 `get_context.py` 显示当前任务：

1. 阅读任务的 `prd.md` 以了解目标
2. 检查 `task.json` 获取当前状态和阶段
3. 询问用户："继续处理 <task-name>？"

如果是，从适当的步骤恢复（通常是步骤 7 或 8）。

---

## 技能参考

### 用户技能 `[USER]`

| 技能 | 使用时机 |
|---------|-------------|
| `$start` | 开始会话（此技能） |
| `$finish-work` | 提交更改之前 |
| `$record-session` | 完成任务后 |

### AI 脚本 `[AI]`

| 脚本 | 用途 |
|---------|---------|
| `python3 ./.trellis/scripts/get_context.py` | 获取会话上下文 |
| `python3 ./.trellis/scripts/task.py create` | 创建任务目录 |
| `python3 ./.trellis/scripts/task.py init-context` | 初始化 jsonl 文件 |
| `python3 ./.trellis/scripts/task.py add-context` | 添加规范到 jsonl |
| `python3 ./.trellis/scripts/task.py start` | 设置当前任务 |
| `python3 ./.trellis/scripts/task.py finish` | 清除当前任务 |
| `python3 ./.trellis/scripts/task.py archive` | 归档已完成的任务 |

### 工作流阶段 `[AI]`

| 阶段 | 目的 | 上下文来源 |
|-------|---------|----------------|
| research | 分析代码库 | 直接仓库检查 |
| implement | 编写代码 | `implement.jsonl` |
| check | 审查和修复 | `check.jsonl` |
| debug | 修复特定问题 | `debug.jsonl` |

---

## 关键原则

> **代码规范上下文是注入的，不是记住的。**
>
> 任务工作流确保 agent 自动接收相关代码规范上下文。
> 这比希望 AI"记住"约定更可靠。
